{
  "collectionName": "components_shared_features",
  "info": {
    "displayName": "Feature",
    "icon": "star",
    "description": "Individual feature item for features sections"
  },
  "options": {},
  "attributes": {
    "icon": {
      "type": "enumeration",
      "enum": [
        "checkmark",
        "shield",
        "clock",
        "users",
        "chart",
        "star",
        "lock",
        "globe",
        "briefcase",
        "award",
        "trending-up",
        "file-check",
        "message-circle",
        "zap",
        "target",
        "layers"
      ],
      "required": true
    },
    "heading": {
      "type": "string",
      "required": true,
      "maxLength": 50
    },
    "description": {
      "type": "text",
      "required": true,
      "maxLength": 150
    },
    "link": {
      "type": "string"
    },
    "linkLabel": {
      "type": "string",
      "maxLength": 25
    }
  }
}
